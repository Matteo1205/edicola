Flask==3.1.3            # Il framework web principale, la base su cui costruisci la tua applicazione.
blinker==1.9.0          # Gestisce i "segnali" interni per far comunicare tra loro le diverse parti dell'app.
click==8.4.0            # Permette di creare comandi personalizzati da usare nel terminale (es. "flask run").
itsdangerous==2.2.0     # Protegge i dati sensibili, come i cookie, rendendoli sicuri e non manomettibili.
Jinja2==3.1.6           # Il motore che ti permette di inserire variabili Python all'interno dei file HTML.
MarkupSafe==3.0.3       # Pulisce il testo nelle pagine web per prevenire attacchi hacker (iniezione di codice).
psycopg2-binary==2.9.12 # Il driver che permette a Python di collegarsi e comunicare con un database PostgreSQL.
python-dotenv==1.2.2    # Carica in automatico le password e le chiavi segrete salvate nel file ".env".
Werkzeug==3.1.8         # Il "motore" di basso livello che aiuta Flask a gestire le richieste web e gli URL.